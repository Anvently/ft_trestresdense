Failles possibles :
	- si on obtient un token valide pour un user "xyz", on supprime le compte, et quelqu'un recreait le compte "xyz", notre token est toujours valide.
	
TODO:

	Backend:
	- implement 2FA authentification

	Devops:
	- implement correct order for docker compose
		- add healthcheck for django app
		- filter healtcheck views in logs
	- dig into production server

	Frontend:
	- implement 2FA 
	- local turnament

	Pong:
		- display explicit message on the page when the lobby doesn't exist
		- redirect (automatically ?) players to matchmaking at the end of a game


BUGS:

- post user batch in pong.js

- the crash in pong is caused by an infinite loop in calculate_impact()

- when a host is invited to join a lobby, the lobby is not cancelled